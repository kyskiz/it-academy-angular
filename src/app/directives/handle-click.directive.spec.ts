import { HandleClickDirective } from './handle-click.directive';

describe('HandleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleClickDirective();
    expect(directive).toBeTruthy();
  });
});
