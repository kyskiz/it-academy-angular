import { SortPaymentsPipe } from './sort-payments.pipe';

describe('SortPaymentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPaymentsPipe();
    expect(pipe).toBeTruthy();
  });
});
